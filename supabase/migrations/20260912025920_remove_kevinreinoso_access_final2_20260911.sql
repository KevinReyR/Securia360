-- Historical operational migration applied remotely before this file existed.
-- Its tenant and account were removed from the hosted project. Fresh local
-- databases have no corresponding records, so only the version is retained.
select 1;
