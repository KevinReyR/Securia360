import {describe,expect,it} from "vitest"; import {planSchema,resourceSchema} from "./schemas";
describe("emergency schemas",()=>{it("rejects negative stock",()=>expect(resourceSchema.safeParse({site_id:"00000000-0000-4000-8000-000000000001",resource_type:"first_aid",name:"Botiquín",quantity:-1,location_description:null,inspection_due_at:null,expires_at:null}).success).toBe(false));it("accepts a versioned plan",()=>expect(planSchema.safeParse({site_id:"00000000-0000-4000-8000-000000000001",version_number:1,summary:"Plan operativo de emergencia actualizado.",effective_from:null,effective_to:null}).success).toBe(true));});
